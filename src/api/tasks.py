I'll apply the suggested edit to the `trigger_article_generation` endpoint. The change involves updating the import path from `src.workers.scraper_tasks` to `src.workers.article_tasks` for the `generate_article_task`.

Here's the updated code with the change applied:

